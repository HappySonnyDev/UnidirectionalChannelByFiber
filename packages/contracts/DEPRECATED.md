# ⚠️ DEPRECATED

This package (`packages/contracts`) is **deprecated** and no longer actively maintained.

The project has migrated to using `@fiber-pay/sdk` and `@nervosnetwork/fiber-js` for payment channel functionality. The contract logic previously managed here is now handled by the Fiber SDK.

This directory is preserved for historical reference and git history. Do not add new code to this package.
